# 🌳 Cây Liên Kết (Linktree) Cá Nhân bằng HTML & CSS

Chào mừng bạn đến với dự án "Linktree" cá nhân của tôi! Đây là một trang web tĩnh đơn giản, được xây dựng hoàn toàn bằng **HTML** và **CSS**, dùng để tổng hợp tất cả các liên kết mạng xã hội và các trang quan trọng khác của Câu lạc bộ CMC ở một nơi duy nhất.

Trang này được thiết kế để nhẹ, nhanh và dễ dàng tùy chỉnh.

## 🖼️ Xem Trước (Demo)

Bạn có thể xem phiên bản trực tiếp tại đây: **[https://mrowl715.github.io/CBT-linktree/]**

<img width="1919" height="917" alt="image" src="https://github.com/user-attachments/assets/6ce3eecf-722a-4d6e-9293-fa15e99fafa3" />


---

## ✨ Tính Năng

* **Thiết kế Tối giản:** Giao diện sạch sẽ, tập trung vào các liên kết.
* **Siêu nhẹ:** Không sử dụng JavaScript hay framework, đảm bảo tốc độ tải trang nhanh nhất.
* **Dễ Tùy Chỉnh:** Dễ dàng thay đổi màu sắc, phông chữ và thêm/bớt liên kết mới.
* **Responsive:** Hiển thị tốt trên cả thiết bị di động và máy tính để bàn.

---

## 🛠️ Công Nghệ Sử Dụng

Dự án này được xây dựng chỉ với các công nghệ web cơ bản:

* ![HTML5]
* ![CSS3]

---

## 🚀 Cách Sử Dụng & Tùy Chỉnh

Bạn hoàn toàn có thể sử dụng mã nguồn này để tạo trang liên kết cho riêng mình.

1.  **Fork** hoặc **Clone** repository này về máy của bạn.
2.  Mở tệp `index.html`.
3.  **Thay đổi thông tin cá nhân:**
    * Thay thế ảnh đại diện (avatar) bằng link ảnh của bạn.
    * Thay đổi tên hiển thị (ví dụ: `@TenCuaBan`).
    * Thay đổi phần mô tả ngắn (bio).
4.  **Cập nhật các liên kết:**
    * Tìm đến phần chứa danh sách các liên kết (thường là trong thẻ `<ul>` hoặc `<nav>`).
    * Thay đổi thuộc tính `href` của mỗi thẻ `<a>` để trỏ đến trang mạng xã hội của bạn.
    * Thay đổi **văn bản** bên trong thẻ `<a>` để mô tả liên kết (ví dụ: "Facebook", "GitHub", "My Blog").
5.  **(Tùy chọn) Chỉnh sửa giao diện:**
    * Mở tệp `style.css` (hoặc `styles.css`, v.v.).
    * Bạn có thể thay đổi màu nền, màu chữ, phông chữ, và hiệu ứng hover tại đây.
6.  **Triển khai (Deploy):**
    * Cách dễ nhất là sử dụng **GitHub Pages**. Chỉ cần vào mục `Settings` > `Pages` trong repo của bạn và chọn nhánh `main` (hoặc `master`) để xuất bản.

---

## 📄 Giấy Phép

Mã nguồn này hoàn toàn miễn phí, bạn có thể sử dụng cho bất kỳ mục đích nào.

---

Cảm ơn bạn đã ghé thăm! 👋
